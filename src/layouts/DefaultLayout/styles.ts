import styled from 'styled-components'

export const LayoutContainer = styled.div`
  width: calc(100vw - 2rem);
  max-width: 70rem; // Equal to 1120px
  margin: 0 auto;

  display: flex;
  flex-direction: column;
`
